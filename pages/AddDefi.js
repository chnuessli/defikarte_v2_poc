import React from 'react';
/**
 * 
 * @returns leitet uns auf die Formulars Seite
 */

const AddDefi = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Formluar</h1>
    </div>
  );
};

export default AddDefi;