import React from 'react';

/**
 * 
 * @returns Zeigt uns den Content der "Die-App" Seite
 */
const DieApp = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '90vh'
      }}
    >
      <h1>Die App</h1>
    
    </div>
  );
};

export default DieApp;